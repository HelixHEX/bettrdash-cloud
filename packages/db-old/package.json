{
  "name": "@bettrdash/db2",  
  "version": "0.0.1",
  "private": true,
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "db:migrate:dev": "prisma migrate dev",
    "db:pre-build": "prisma migrate deploy && npm run db:generate",
    "db-generate": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.8.0",
    "dotenv-safe": "^8.2.0"
  },
  "devDependencies": {
    "config": "*",
    "prisma": "^5.8.0",
    "tsconfig": "*",
    "typescript": "^4.5.3"
  }
}
