{
  "name": "db",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "db:migrate:dev": "yarn prisma migrate dev",
    "db:generate": "yarn prisma generate",
    "db:pre-build": "yarn db:generate"
  },
  "dependencies": {
    "@prisma/client": "^4.13.0",
    "dotenv-safe": "^8.2.0",
    "prisma": "4.13.0"
  },
  "devDependencies": {
    "config": "*",
    "tsconfig": "*",
    "typescript": "^4.5.3"
  }
}
